# SelectionSortAlgorithm
This C++ program implements the Selection Sort algorithm to sort an array in ascending order. Selection Sort repeatedly selects the smallest element from the unsorted part of the array and swaps it with the first unsorted position.
